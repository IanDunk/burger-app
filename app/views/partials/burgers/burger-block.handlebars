<li>
	{{name}}

	<button class="devour-it" data-id="{{id}}" data-newsleep="{{sleep}}">
		{{#if sleep}}DEVOUR IT!{{else}}THROW IT UP!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
